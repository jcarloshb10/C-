#include <iostream>
using namespace std;
main()
{

	long l1,l2,condicionlogica,expresionlogica,variabledecontrol,valor1;
	unsigned long ul1;
	double d1,d2;
	
	char c1,c2;
	unsigned char uc1; //DEL 33 AL 82
	
	bool b1,b2,b3; //(banderas) .. acepta dos valores ( 0(falso),1(verdadero) )
	b1=false;
	b2=true;
	
	// LOS COMPARADORES LOGICOS SON: ºMAYOR QUE (>); ºMENOR QUE (<); ºIGUAL A (==); ºDIFERENTE A (!=)
	
	// LOS OPERADORES LOGICOS SON: 1) && "y" LOGICO;  2) ||(ALT+124) "o" logico.;  3) ! negacion logica;
	
	if(condicionlogica)
	{
	 	// Acciones por verdadero
	}
	else
	{
	// Acciones por falso	
	}
	
	
	//Repetir (hacer hasta que):
	do
	{
		
	}
	while (expresionlogica);
	
	//Estructura (While)
	while(expresionlogica)
	{
		
	}
	
	// Variable de seleccion multiple(switch):
	switch(variabledecontrol)
	{
		case 1: //ese 1 es EL VALOR #1
			// Acciones
			break;
	}
}
