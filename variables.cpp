#include <iostream>
using namespace std;
main()
{
	short s1,s2,condicionlogica,expresionlogica,variabledecontrol,valor1;
	int i1,i2;
	long l1,l2; // Maneja numeros negativos. // UTILIZAR ÈSTA
	unsigned long ul1; //Para definir el sigo (aquì el valor de lA variable long es positiva).// UTILIZAR ÈSTA
	
	float f1,f2; //real corto
	double d1,d2; // Real largo. // UTILIZAR ÈSTA
	long double ld1;// Numeros muy largos con enteros y reales.
	
	char c1,c2;
	unsigned char uc1; //DEL 33 AL 82
	
	bool b1,b2,b3; //(banderas) .. acepta dos valores ( 0(falso),1(verdadero) )
	b1=false;
	b2=true;
	
	// LOS COMPARADORES LOGICOS SON: ºMAYOR QUE (>); ºMENOR QUE (<); ºIGUAL A (==); ºDIFERENTE A (!=)
	
	// LOS OPERADORES LOGICOS SON: 1) && "y" LOGICO;  2) ||(ALT+124) "o" logico.;  3) ! negacion logica;
	
	if(condicionlogica)
	{
	 	// Acciones por verdadero
	}
	else
	{
	// Acciones por falso	
	}
	
	
	//Repetir (hacer hasta que):
	do
	{
		
	}
	while (expresionlogica);
	
	//Estructura (While)
	while(expresionlogica)
	{
		
	}
	
	// Variable de seleccion multiple(switch):
	switch(variabledecontrol)
	{
		case 1://el 1 es el VALOR #1
			// Acciones
			break;
	}
	
}	
